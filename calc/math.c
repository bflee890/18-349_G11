#include <stdio.h>
#include "math.h"

int add(a, b) {
  return a + b;
}

int subtract(a, b) {
  return a - b;
}

int multiply(a, b) {
  return a * b;
}

int divide(a, b) {
  return a / b;
}

int modulo(a, b) {
  return a % b;
}
